#start snapshoting:
#xvfb-run --server-args="-screen 0, 1024x768x24" site2image urls


#The Proxy Credential File must have the following format:
#
# if http auth used
[http_proxy_auth]
username = foo
password = bar

# if https auth used
[https_proxy_auth]
username = foo
password = bar


